<%= form_for(@registration_answer) do |f| %>
  <% if @registration_answer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@registration_answer.errors.count, "error") %> prohibited this registration_answer from being saved:</h2>

      <ul>
      <% @registration_answer.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
