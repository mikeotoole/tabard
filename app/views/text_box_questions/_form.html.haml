= form_for(@text_box_question) do |f|
  - if @text_box_question.errors.any?
    #error_explanation
      %h2
        = pluralize(@text_box_question.errors.count, "error")
        prohibited this text_box_question from being saved:
      %ul
        - @text_box_question.errors.full_messages.each do |msg|
          %li= msg
  .field
    = f.label :content
    %br/
    = f.text_area :content, :rows => 3
  .actions
    = submit_or_cancel(f)
