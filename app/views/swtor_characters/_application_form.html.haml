%h3 New SWTOR Character
= fields_for :swtor_character, @character, :index => Time.now.to_f.to_s do |f|
  = f.hidden_field :game_id
  %p.field
    = f.label :name
    = f.text_field :name
  %p.field
    = f.label :server
    = f.text_field :server