- title "Remove Community Confirmation | #{@community.name}"

%h1 Are you sure you want to remove this community?
%p
  %strong.omg All pages, discussions, comments, and members of will also be removed.
  %br
  This cannot be undone, so be absolutely sure that you want to do this.

= simple_form_for(current_user, :url => community_url(@community, :subdomain => @community.subdomain, :protocol => (Rails.env.development? ? "http://" : "https://")), :html => { :method => :delete }) do |f|
  %fieldset
    %ol
      = f.input :current_password, :label => "Account Password", :hint => "we need your password to confirm this request"
    %p
      %button{ :type => 'submit', 'data-confirm' => 'Last chance... Are you sure you want to do this?' } Remove Community