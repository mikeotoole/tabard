- title "Editing Supported Game | #{current_community.name}"

%h1 Editing Supported Game

= simple_form_for @supported_game, html: {novalidate: true} do |f|
  = f.error_notification
  %fieldset
    %ol
      = f.input :name, input_html: { maxlength: SupportedGame::MAX_NAME_LENGTH }
      - if @supported_game.game.respond_to?(:all_factions)
        = f.input :faction, as: :radio_buttons, collection: @supported_game.all_factions, wrapper_html: { class: 'select with-label faction' }
      - if @supported_game.game.respond_to?(:all_servers)
        = f.input :server_name, as: :radio_buttons, label: 'Server', collection: @supported_game.all_servers, wrapper_html: { class: 'select with-label server' }
      - if @supported_game.game.respond_to?(:all_server_types)
        = f.input :server_type, as: :radio_buttons, collection: @supported_game.all_server_types, wrapper_html: { class: 'select with-label' }
      %li
        %button{ type: 'submit' } Save changes
