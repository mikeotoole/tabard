- title "#{@page.name} | #{@page.page_space.name} | #{current_community.name}"

%nav
  %h1
    = link_to "Page Spaces", page_spaces_path
    - if @page.page_space.game
      %rt= " — #{@page.page_space.game.name}"
    = link_to @page.page_space.name, @page.page_space
    %strong= @page.name

%section
  = @page.body