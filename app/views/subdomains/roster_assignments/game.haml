- title "#{@community_game.smart_name} Roster | #{current_community.name}"

#breadcrumb
  %ol
    %li= link_to "Members", roster_assignments_url
    %li
      %span= "#{@community_game.smart_name} Roster"

%h1= "#{@community_game.smart_name} Roster"
= paginate @member_profiles
= render 'table', member_profiles: @member_profiles, community_game: @community_game
= paginate @member_profiles