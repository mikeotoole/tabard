- title "#{@discussion_space.name} | #{@community.name}"

#breadcrumb
  %ol
    %li= link_to "Discussion Spaces", discussion_spaces_path
    %li
      %span= @discussion_space.name

- if @discussion_space.discussions.any?
  = render "subdomains/discussions/table", :discussion_space => @discussion_space

-else
  %p This discussion space has no viewable discussions.
  - if (can? :create, @discussion_space.discussions.new)
    %p= link_to 'Create a Discussion', new_discussion_space_discussion_path(@discussion_space), :class => 'button'
