- title "Editing Discussion Space | #{@community.name}"

%h1 Editing Discussion Space

= simple_form_for @discussion_space do |f|
  = render 'fields', :f => f
  %p.submit
    = f.button :submit, 'Save Changes'