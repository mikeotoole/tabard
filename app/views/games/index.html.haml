%p#bcrumb
  = link_to 'Games', :games
  
%h1 Games
  
%table
  - if @games.size > 0
    %thead
      %tr
        %th.name Name
        %th.amount Characters
        %th.communities Communities
    %tbody
      - @games.each do |game|
        %tr
          %td.name
            = link_to game.name, game_path(game)
          %td.amount
            = game.characters.size.to_s
          %td.communities
            = game.communities.size.to_s
  - else
    %tbody
      %tr
        %td{ :colspan => 3 } There are currently no games.