- title 'Home'
= stylesheet_link_tag "application/home"

#homebox

  %h1 Get your guild on the same page with a powerful tool that simplifies community management.
  %p
    = link_to 'Learn more', top_level_intro_url
    or
    = link_to 'signup', new_user_registration_url
    for your free account.

  %ul.slideshow
    %li Derp 1
    %li Derp 2
    %li Derp 3
    %li Derp 4
    %li Derp 5

  = simple_form_for new_user_session_url(:subdomain => "secure", :protocol => (Rails.env.development? ? "http://" : "https://")), :html_input => { class: 'login' } do |f|
    %fieldset
      %ol
        = f.input :email
        = f.input :password
        = f.input :remember_me, :as => :boolean
        %li
          %button{ :type => 'submit' } Login

  = simple_form_for new_user_registration_url(:subdomain => "secure", :protocol => (Rails.env.development? ? "http://" : "https://")), :html_input => { class: 'signup' } do |f|
    %fieldset
      %ol
        = f.input :email, :as => :email
        = f.simple_fields_for :user_profile do |uf|
          = uf.input :display_name, :required => false
        = f.input :password
        %li
          %button{ :type => 'submit' } Sign up