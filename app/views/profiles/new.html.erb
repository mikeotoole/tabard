<h1>New profile</h1>
<!-- Having the user and game profiles sharing views may create problems. This will be used for the creation of new  -->
<%= form_for(@profile) do |f| %>
  <% if @profile.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@profile.errors.count, "error") %> prohibited this profile from being saved:</h2>

      <ul>
      <% @profile.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<% if false %>
<!-- This will enable the selection of the type of profile to create. We are not using this. 
The User will select what game they want a profile for. -->
  <div class="field">
    Type: <br />
    <%= f.select( :type_helper, Profile.select_options) %>
  </div>
<% end %>

    Type: <%= @profile.type_helper %>
    <%= f.hidden_field :type_helper, :value => @profile.type_helper%>
  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

<%= link_to 'Back', profiles_path %>
