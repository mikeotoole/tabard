%p#notice= notice
%p
  %b Game:
  = render @game if @game
%p
  %b Name:
  = @character.name
= link_to 'User Profile', user_profile_path(UserProfile.find(current_user))
|
\#{link_to 'Edit', edit_wow_character_path(@character)} |
\#{js_back}
