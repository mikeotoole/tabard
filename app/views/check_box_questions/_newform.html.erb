<%= form_for(@check_box_question, :remote => true) do |f| %>
  <% if @check_box_question.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@check_box_question.errors.count, "error") %> prohibited this check_box_question from being saved:</h2>

      <ul>
      <% @check_box_question.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  
  <div class="field">
    <%= f.hidden_field :site_form_id %>
  </div>
  
  <div class="field">
    <%= f.label :content %><br />
    <%= f.text_area :content %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
