- discussion_space = DiscussionSpace.find_by_id(activity.target_id)
- case activity.action
- when 'created'
  Created a new
- when 'edited'
  Edited the
%strong= link_to_if (user_signed_in? and current_user.in_community(activity.community).can? :show, discussion_space), 'discussion space', discussion_space_url(discussion_space, :subdomain => activity.community_subdomain), :meta => discussion_space.name
- unless defined? current_community and activity.community == current_community
  in
  %strong= link_to activity.community_name, root_url(:subdomain => activity.community_subdomain)