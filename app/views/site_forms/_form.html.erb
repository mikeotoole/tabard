<%= form_for(@site_form) do |f| %>
  <% if @site_form.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@site_form.errors.count, "error") %> prohibited this site_form from being saved:</h2>

      <ul>
      <% @site_form.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :message %><br />
    <%= f.text_area :message %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
