<%= form_for([@comment], :remote => true, :html => {:style => 'display: none;'}) do |f| %>
  <div class="field"> 
    <%= f.label :body %><br /> 
    <%= f.text_area :body %>
  </div> 
  <div class="actions">
    <%= f.submit 'Add Comment' %> <%= link_to "Cancel", "#", :onClick => "$('#new_comment').detach(); $('.new_comment_link').show();" %>
  </div>
<% end %>
