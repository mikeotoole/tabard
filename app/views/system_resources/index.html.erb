<h1>Listing system_resources</h1>

<table>
  <tr>
    <th>Name</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @system_resources.each do |system_resource| %>
  <tr>
    <td><%= system_resource.name %></td>
    <td><%= link_to 'Show', system_resource %></td>
    <td><%= link_to 'Edit', edit_system_resource_path(system_resource) %></td>
    <td><%= link_to 'Destroy', system_resource, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New System resource', new_system_resource_path %>
