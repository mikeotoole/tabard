<%= form_for(@system_resource) do |f| %>
  <% if @system_resource.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@system_resource.errors.count, "error") %> prohibited this system_resource from being saved:</h2>

      <ul>
      <% @system_resource.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
