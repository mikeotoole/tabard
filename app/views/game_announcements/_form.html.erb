<%= form_for(@game_announcement) do |f| %>
  <% if @game_announcement.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@game_announcement.errors.count, "error") %> prohibited this game_announcement from being saved:</h2>

      <ul>
      <% @game_announcement.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :body %><br />
    <%= f.text_area :body %>
  </div>
  <div class="field">
    <%= f.label :game_id %><br />
	<%= f.collection_select(:game_id, Game.where("is_active = ?", true), :id, :name, options ={:prompt => "Select a game"}, :class =>"game") %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
