<%= div_for game_announcement do %>
  <h3>
    <%= link_to game_announcement.title, game_announcement %> 
      <span class='actions'>
      	<%= link_to( 'Edit', edit_game_announcement_path(game_announcement)) %>
        <%= link_to( 'Delete', game_announcement, :confirm => 'Are you sure?', :method => :delete) %>
      </span>
  </h3>
<% end %>