<%= form_for(@page_space) do |f| %>
  <% if @page_space.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@page_space.errors.count, "error") %> prohibited this page_space from being saved:</h2>

      <ul>
      <% @page_space.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
