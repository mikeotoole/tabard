<%= form_for(@teamspeak) do |f| %>
  <% if @teamspeak.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@teamspeak.errors.count, "error") %> prohibited this teamspeak from being saved:</h2>

      <ul>
      <% @teamspeak.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
