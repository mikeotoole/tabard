%p
  You have
  %strong= @characterCount
  character profiles.

- if @characterCount > 0

  #characters
    - if @wowCharacters
      - @wowCharacters.each do |character|
        - @character = character
        = simple_fields_for :wow_character, @character, :index => Time.now.to_f.to_s do |f|
          = render :file => 'wow_characters/_form_fields.html.haml', :locals => { :f => f }
    - if @swtorCharacters
      - @swtorCharacters.each do |character|
        - @character = character
        = simple_fields_for :swtor_character, @character, :index => Time.now.to_f.to_s do |f|
          = render :file => 'swtor_characters/_form_fields.html.haml', :locals => { :f => f }
  %p
    %button{ :type => 'submit' }
      Save all characters

- if @games
  #newcharacter
    %dl.selectbox
      %dt Add a new character for&hellip;
      - @games.each do |game|
        %dd
          = link_to game.name, new_base_character_path(:game => {:game_id => game.id})
