%p
  Hello #{@resource.email}!
%p Someone has requested a link to reinstate your account, and you can do this through the link below.
%p= link_to 'Reinstate account', reinstate_account_url(:reset_password_token => @resource.reset_password_token)
%p If you didn't request this, please ignore this email.
%p Your account will not be reinstated until you access the link above and create a new password.