%h1 Listing roles

%table
  %tr
    %th Community
    %th Name
    %th System generated
    %th
    %th
    %th

  - @roles.each do |role|
    %tr
      %td= role.community_id
      %td= role.name
      %td= role.system_generated
      %td= link_to 'Show', role
      %td= link_to 'Edit', edit_role_path(role)
      %td= link_to 'Destroy', role, :confirm => 'Are you sure?', :method => :delete

%br

= link_to 'New Role', new_role_path
