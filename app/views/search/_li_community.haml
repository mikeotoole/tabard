%li.community
  %h3
    = link_to community.name, root_url(subdomain: community.subdomain)
    = " — #{pluralize community.get_current_community_roster.size, 'member', 'members'}"
  - if community.games
    %span.games
      %strong Games
      - community.games.each do |game|
        = link_to game.short_name, search_url(term: game.short_name)