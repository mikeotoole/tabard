%h1 New #{@character.game_name.to_s} Character

= simple_form_for [@played_game, @character], html: {novalidate: true} do |f|
  %fieldset
    %ol
      = render "characters/#{@played_game.game_type.downcase}/fields", f: f, character: @character
    .submit
      %button{ type: 'submit' } Create character