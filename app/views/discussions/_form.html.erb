<%= form_for(@discussion) do |f| %>
  <% if @discussion.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@discussion.errors.count, "error") %> prohibited this discussion from being saved:</h2>

      <ul>
      <% @discussion.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :body %><br />
    <%= f.text_area :body %>
  </div>
  <div class="field">
    <%= f.label :discussion_space_id %><br />
    <%= f.text_field :discussion_space_id %>
  </div>
  <div class="field">
    <%= f.label :character_id %><br />
    <%= f.text_field :character_id %>
  </div>
  <div class="field">
    <%= f.label :user_profile_id %><br />
    <%= f.text_field :user_profile_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
