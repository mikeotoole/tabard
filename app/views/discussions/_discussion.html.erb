<%= div_for discussion do %>
  <%= link_to discussion.discussion_space.name, discussion.discussion_space %>
  <h3><%= discussion.name %></h3>
  <p>By: <%= discussion.user_profile %></p>
  <p>By: <%= discussion.character %></p>
  <p>Body: <%= discussion.body %></p>
  <h4>Comments</h4>
  <ul>
    <% discussion.comments.each do  |comment| %>
      <%= render comment %>
    <% end %>
  </ul>
  <span class='actions'>
    <td><%= (logged_in? and current_user.can_update(discussion)) ? link_to( 'Edit', edit_discussion_path(discussion)) : "No Edit Permissions" %></td>
      <td><%= (logged_in? and current_user.can_delete(discussion)) ? link_to( 'Delete', discussion, :confirm => 'Are you sure?', :method => :delete) : "No Delete Permissions" %></td>
  </span>
<% end %>
