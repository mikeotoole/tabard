!!!
%html

  %head
    %title= yield_for(:title, 'Crumblin')
    = stylesheet_link_tag "crumblin/crumblin"
    = javascript_include_tag "application"
    = csrf_meta_tags

  %body
    = render 'layouts/bar_' + (user_signed_in? ? 'in' : 'out')
    = render 'crumblin/pitch' if show_pitch?
    = render 'layouts/flash_messages'
    #content
      = yield
    = render 'layouts/footer'