<h1>Listing site_announcements</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @site_announcements.each do |site_announcement| %>
  <tr>
    <td><%= link_to 'Show', site_announcement %></td>
    <td><%= link_to 'Edit', edit_site_announcement_path(site_announcement) %></td>
    <td><%= link_to 'Destroy', site_announcement, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Site announcement', new_site_announcement_path %>
