# Sample localization file for English. Add more files in this directory for other locales.
# See http://github.com/svenfuchs/rails-i18n/tree/master/rails%2Flocale for starting points.

en:
  time:
    formats:
      time_12: "%l:%M%p"
      time_24: "%H:%M"
      date_short: "%b %e, %Y"
      date_monthday: "%b %e"
  hello: "Hello world"
  activerecord:
    error:
      message:
        taken: "has already been taken"
    errors:
      models:
        artwork_upload:
          attributes:
            artwork_image:
              size_too_big: "image file size is too large (should be at most 2MB)"
        community:
          attributes:
            name:
              existing_name: "is too similar to an existing community"
              restricted_name: "is not allowed for a community"
              profanity: "is not allowed"
              restriced: "is not allowed"
            background_image:
              size_too_big: "image file size is too large (should be at most 2MB)"
        user_profile:
          attributes:
            display_name:
              restriced: "is not allowed"
            avatar:
              size_too_big: "image file size is too large (should be at most 2MB)"
        swtor_character:
          attributes:
            avatar:
              size_too_big: "image file size is too large (should be at most 2MB)"
        wow_character:
          attributes:
            avatar:
              size_too_big: "image file size is too large (should be at most 2MB)"
  community:
    default:
      page_space:
        name: "General"
      home_page:
        name: "Home"
        markup: "**This page is displayed to users who visit your community unless they are not logged in or not a member.** It exists within the wiki pages section of your site. You can select which page is set as the home page when editing your community settings. Make sure to edit this page before recruiting your members."