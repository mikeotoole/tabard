require 'spec_helper'

describe ActivitiesController do

  describe "GET 'index'" do
    it "assigns 10 most recent activities and comments as @items" do
      pending "This needs to have a js test written for it."
      #create(:comment)
      #create(:activity)
      #recentComment = create(:comment)
      #recentActvity = create(:activity)
      #get :index
      #assigns(:items)[0].should eq(recentActvity)
      #assigns(:items)[1].should eq(recentComment)
    end
  end

end
